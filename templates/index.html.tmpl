<html>
    <p><b>Load Avg:</b> {{ .LoadAvg1 }} / {{ .LoadAvg5 }} / {{ .LoadAvg15 }}</p>
    <p><b>CPU Temp:</b> {{ .CPUTemp }} °C</p>
    {{ range $name, $b64 := .Plots }}
        <img alt="{{ $name }}" src="data:image/png;base64,{{ $b64 }}" />
    {{ end }}
</html>